{
  "name": "staffsync-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "mongoose": "^8.7.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "@types/node": "^22.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "dev": "npm run dev:frontend",
    "start": "npm run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:vercel": "vercel dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "seed": "echo Visit /api/seed in your browser after starting the server, or run: npx ts-node --esm backend/seed.ts",
    "install:all": "npm install && npm --prefix frontend install",
    "postinstall": "echo Installation complete. Run \"npm run dev\" to start the frontend dev server."
  }
}
